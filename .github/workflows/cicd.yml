name: CICD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  on_build_actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run Main
        run: python hypedsearch.py
        working-directory: src/hypedsearch
      - name: Run Tests
        run: python -m unittest test_hypedsearch
        working-directory: src/test
