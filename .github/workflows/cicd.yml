name: CICD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  on_build_actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run Main
        run: python main.py
        working-directory: src
      #- name: Run Tests
      #  run: tests.py
      #  working-directory: src  
