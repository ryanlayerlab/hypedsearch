name: CICD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  on_build_actions:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Dependencies
        run: |
          cd $GITHUB_WORKSPACE
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      #- name: Update Environment Varaible
      #  run: |
      #    echo "PYTHONPATH=home/runner/work/hypedsearch/src" >> $GITHUB_ENV
      - name: Set the value
        id: step_one
        run: |
          echo "action_state=yellow" >> $GITHUB_ENV
      - name: Use the value
        id: step_two
        run: |
          echo "${{ sys.path }}" 
       #  echo "${{ env.GITHUB_PATH }}" #returns nothing
       #  echo "${{ sys.path }}" #returns nothing
       #  echo "${{ env.action_state }}" #returns yellow
      # - name: Run Tests
      #  run: |
      #    python -m unittest test_hypedsearch
      #  working-directory: test
