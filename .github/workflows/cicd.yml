name: CICD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  on_build_actions:
    runs-on: ubuntu-latest
    steps:
      - name: Run Main
        run: main.py
        working-directory: src
      #- name: Run Tests
      #  run: tests.py
      #  working-directory: src  
